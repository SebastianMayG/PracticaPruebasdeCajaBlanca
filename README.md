# Como ejecutarlo
python -m venv .venv
source .venv/bin/activate   o .venv\Scripts\activate en Windows
pip install -r requirements.txt

Ejecutar la clase Main o en la terminal ejecutar coverage run -m pytest .\procesar_credenciales_test.py
